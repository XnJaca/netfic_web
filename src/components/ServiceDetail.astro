---
interface Props {
  title: string;
  description: string;
  icon: string;
  features: string[];
  benefits: string[];
  isNew?: boolean;
  image: string;
}

const {
  title,
  description,
  icon,
  features,
  benefits,
  isNew = false,
  image,
} = Astro.props;
---

<div class="py-12 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="lg:flex lg:items-center lg:justify-between">
      <div class="lg:w-1/2 lg:pr-8">
        <div class="relative">
          {
            isNew && (
              <span class="absolute top-0 right-0 bg-green-500 text-white text-sm font-bold px-3 py-1 rounded-bl-lg">
                NUEVO
              </span>
            )
          }
          <div
            class="inline-flex items-center justify-center rounded-md bg-indigo-100 p-4 mb-4"
          >
            <Fragment set:html={icon} />
          </div>
          <h1 class="text-3xl font-bold text-gray-900 sm:text-4xl mb-6">
            {title}
          </h1>
          <p class="text-lg text-gray-500 mb-8">{description}</p>

          <h2 class="text-xl font-semibold text-gray-800 mb-4">
            Características principales:
          </h2>
          <ul class="mb-8 space-y-3">
            {
              features.map((feature) => (
                <li class="flex items-start">
                  <div class="flex-shrink-0">
                    <svg
                      class="h-5 w-5 text-green-500"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M5 13l4 4L19 7"
                      />
                    </svg>
                  </div>
                  <p class="ml-3 text-base text-gray-600">{feature}</p>
                </li>
              ))
            }
          </ul>

          <h2 class="text-xl font-semibold text-gray-800 mb-4">Beneficios:</h2>
          <ul class="mb-8 space-y-3">
            {
              benefits.map((benefit) => (
                <li class="flex items-start">
                  <div class="flex-shrink-0">
                    <svg
                      class="h-5 w-5 text-indigo-500"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 6v6m0 0v6m0-6h6m-6 0H6"
                      />
                    </svg>
                  </div>
                  <p class="ml-3 text-base text-gray-600">{benefit}</p>
                </li>
              ))
            }
          </ul>

          <div class="mt-10">
            <a
              href="/contacto"
              class="inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-semibold px-6 py-3 rounded-lg transition duration-300"
            >
              Solicitar información
            </a>
          </div>
        </div>
      </div>
      <div class="lg:w-1/2 mt-10 lg:mt-0">
        <div class="rounded-lg overflow-hidden shadow-xl">
          <img
            src={image}
            alt={`${title} - NetFic Solutions`}
            class="w-full h-auto"
          />
        </div>
      </div>
    </div>

    <div class="mt-16 pt-8 border-t border-gray-200">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">
        ¿Cómo podemos ayudarte?
      </h2>
      <p class="text-lg text-gray-500 mb-6">
        En NetFic Solutions entendemos las necesidades específicas de cada
        cliente. Nuestro equipo de profesionales está preparado para asesorarte
        y brindarte la mejor solución adaptada a tus requerimientos.
      </p>
      <div class="flex flex-wrap gap-4">
        <a
          href="/contacto"
          class="inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-semibold px-6 py-3 rounded-lg transition duration-300"
        >
          Contactar ahora
        </a>
        <a
          href="/servicios"
          class="inline-block border border-gray-300 hover:border-gray-400 text-gray-700 font-semibold px-6 py-3 rounded-lg transition duration-300"
        >
          Ver todos los servicios
        </a>
      </div>
    </div>
  </div>
</div>
